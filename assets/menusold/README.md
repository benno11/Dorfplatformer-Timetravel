# Deprecated Menus

This folder contains the pre-slim-menu JSON assets kept for compatibility with deprecated menu paths.

New menus live in `assets/menus` as `.menu` files. Title screen and pause remain native.
