set(VCPKG_TARGET_ARCHITECTURE arm64)
set(VCPKG_CRT_LINKAGE dynamic)
set(VCPKG_LIBRARY_LINKAGE static)
set(VCPKG_CMAKE_SYSTEM_NAME iOS)
set(VCPKG_OSX_ARCHITECTURES arm64)
set(VCPKG_OSX_SYSROOT iphoneos)
if(DEFINED ENV{PLATFORMER_IOS_DEPLOYMENT_TARGET} AND NOT "$ENV{PLATFORMER_IOS_DEPLOYMENT_TARGET}" STREQUAL "")
  set(VCPKG_OSX_DEPLOYMENT_TARGET "$ENV{PLATFORMER_IOS_DEPLOYMENT_TARGET}")
else()
  set(VCPKG_OSX_DEPLOYMENT_TARGET 15.0)
endif()
